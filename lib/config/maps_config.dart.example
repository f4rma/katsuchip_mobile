class MapsConfig {
  // Google Maps API Key
  // Get your key from: https://console.cloud.google.com/apis/credentials
  // Biarkan kosong ('') untuk menonaktifkan dan gunakan fallback Nominatim (OSM).
  static const String googleMapsApiKey = 'YOUR_GOOGLE_MAPS_API_KEY';
}
